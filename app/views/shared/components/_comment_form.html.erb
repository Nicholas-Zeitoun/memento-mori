<% @comment = Comment.new %>
<%= simple_form_for([@meme, @comment], remote: true) do |f| %>
  <%= f.input :content, label: false, required: true, placeholder: 'New comment...' %>
  <br>
  <%= f.submit 'submit', class: "btn btn-primary text-white" %>
<% end %>
